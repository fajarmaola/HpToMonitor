<Window x:Class="SecondScreen.Desktop.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" ShowInTaskbar="False"
        Width="440" Height="320" FontFamily="Segoe UI Variable, Segoe UI">
    <Border CornerRadius="22" Background="{StaticResource WindowBg}"
            BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.6" Color="#000000"/>
        </Border.Effect>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="assets/logo.png" Width="128" Height="128"/>
            <TextBlock Text="HP ke Monitor" Foreground="{StaticResource Fg}" FontSize="26"
                       FontWeight="Bold" HorizontalAlignment="Center" Margin="0,16,0,0"/>
            <TextBlock Text="PT Teleraya Digital Group" Foreground="{StaticResource Accent}"
                       FontSize="12.5" HorizontalAlignment="Center" Margin="0,6,0,0"/>
            <TextBlock Text="company.teleraya.com" Foreground="{StaticResource FgMuted}"
                       FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0"/>
        </StackPanel>
    </Border>
</Window>
